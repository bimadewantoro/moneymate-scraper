# MoneyMate Scraper - Environment Variables
# Copy this file to .env and fill in your values.

# --- Google OAuth ---
# Path to the Google Cloud OAuth credentials file (downloaded from GCP Console).
GOOGLE_CREDENTIALS_PATH=credentials.json

# Path where the OAuth token will be cached after first login.
GOOGLE_TOKEN_PATH=token.json

# --- MoneyMate API ---
# The base URL of your MoneyMate Next.js application API.
MONEYMATE_API_URL=http://localhost:3000/api

# --- General ---
# Logging level: DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_LEVEL=INFO
